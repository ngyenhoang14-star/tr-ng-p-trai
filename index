:::writing{variant="document" id="58321" title="Website sinh nhật bất ngờ cho Thảo"}
<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Một thứ dành cho Thảo</title>

<style>
@import url('https://fonts.googleapis.com/css2?family=Pacifico&family=Quicksand:wght@400;500;600;700&display=swap');

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    overflow: hidden;
    font-family: 'Quicksand', sans-serif;
    background: #f5f5f7;
    color: #333;
}

/* =========================
   MÀN HÌNH
========================= */

.screen {
    position: fixed;
    inset: 0;

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;

    text-align: center;

    opacity: 0;
    visibility: hidden;

    transition: opacity 0.8s ease;
}

.screen.active {
    opacity: 1;
    visibility: visible;
}

/* =========================
   MÀN 1
========================= */

#screen1 {
    background: linear-gradient(
        135deg,
        #f7f7f7,
        #eeeeee
    );
}

.normal-title {
    font-size: 38px;
    font-weight: 700;
    color: #333;
    margin-bottom: 15px;
}

.normal-text {
    font-size: 18px;
    color: #777;
    margin-bottom: 35px;
}

.button {
    border: none;
    background: #333;
    color: white;

    padding: 15px 35px;

    border-radius: 50px;

    font-size: 17px;
    font-weight: 600;

    cursor: pointer;

    transition: 0.3s;
}

.button:hover {
    transform: scale(1.06);
    background: #222;
}

/* =========================
   MÀN 2
========================= */

#screen2 {
    background: #f4f4f5;
}

.loading-title {
    font-size: 28px;
    margin-bottom: 25px;
}

.loading-box {
    width: 280px;
    height: 12px;

    background: #ddd;

    border-radius: 20px;

    overflow: hidden;

    margin-bottom: 15px;
}

.loading-bar {
    width: 0;
    height: 100%;

    background: #555;

    border-radius: 20px;

    transition: width 2.5s linear;
}

.loading-percent {
    color: #888;
    font-size: 14px;
}

#continueBtn {
    display: none;
    margin-top: 30px;
}

/* =========================
   MÀN 3
========================= */

#screen3 {
    background: #f5f5f6;
}

.countdown {
    font-size: 110px;
    font-weight: 700;
    color: #333;

    animation: countdownPop 1s ease;
}

/* =========================
   MÀN CUỐI
========================= */

#screen4 {
    background:
        radial-gradient(
            circle at center,
            #fff0f7 0%,
            #ffd7e8 40%,
            #e8d6ff 100%
        );

    overflow: hidden;
}

.final-small {
    font-size: 20px;
    color: #b05282;
    letter-spacing: 4px;
    margin-bottom: 15px;
}

.final-title {
    font-family: 'Pacifico', cursive;

    font-size: clamp(
        65px,
        12vw,
        150px
    );

    line-height: 1.1;

    background:
        linear-gradient(
            90deg,
            #ff4f9a,
            #b95cff,
            #ff65a7,
            #ff4f9a
        );

    background-size: 300%;

    -webkit-background-clip: text;
    background-clip: text;

    color: transparent;

    animation:
        rainbowText 5s linear infinite,
        titleFloat 3s ease-in-out infinite;

    text-shadow:
        0 10px 35px rgba(220, 70, 150, 0.2);
}

.final-name {
    font-family: 'Pacifico', cursive;

    font-size: clamp(
        55px,
        10vw,
        110px
    );

    color: #d83d87;

    margin-top: 5px;

    text-shadow:
        0 5px 0 white,
        0 10px 25px rgba(200, 50, 130, .2);

    animation: nameBeat 2s infinite;
}

.cake {
    font-size: 70px;
    margin: 15px 0;

    animation: cakeFloat 3s ease-in-out infinite;
}

.message {
    width: min(90%, 650px);

    padding: 22px 30px;

    background: rgba(255,255,255,.55);

    backdrop-filter: blur(15px);

    border-radius: 28px;

    color: #70455f;

    font-size: 18px;

    line-height: 1.7;

    box-shadow:
        0 15px 50px rgba(150,70,130,.15);

    animation: messageAppear 1.5s ease;

    position: relative;
    z-index: 10;
}

.message strong {
    color: #df3f8d;
}


/* =========================
   VẬT THỂ BAY
========================= */

.floating {
    position: fixed;

    pointer-events: none;

    z-index: 5;

    animation: floatUp linear forwards;
}

.confetti {
    position: fixed;

    width: 9px;
    height: 17px;

    border-radius: 3px;

    top: -30px;

    z-index: 4;

    animation: confettiFall linear forwards;
}

.sparkle {
    position: fixed;

    color: white;

    font-size: 20px;

    z-index: 3;

    animation: sparkleAnim 2s ease-in-out infinite;
}


/* =========================
   ANIMATION
========================= */

@keyframes rainbowText {

    0% {
        background-position: 0%;
    }

    100% {
        background-position: 300%;
    }
}

@keyframes titleFloat {

    0%,100% {
        transform: translateY(0);
    }

    50% {
        transform: translateY(-10px);
    }
}

@keyframes nameBeat {

    0%,100% {
        transform: scale(1);
    }

    50% {
        transform: scale(1.06);
    }
}

@keyframes cakeFloat {

    0%,100% {
        transform: translateY(0) rotate(-2deg);
    }

    50% {
        transform: translateY(-12px) rotate(2deg);
    }
}

@keyframes countdownPop {

    0% {
        transform: scale(2);
        opacity: 0;
    }

    100% {
        transform: scale(1);
        opacity: 1;
    }
}

@keyframes messageAppear {

    from {
        opacity: 0;
        transform: translateY(30px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes floatUp {

    0% {
        transform:
            translateY(110vh)
            rotate(0deg);

        opacity: 0;
    }

    10% {
        opacity: 1;
    }

    50% {
        transform:
            translateY(50vh)
            translateX(30px)
            rotate(180deg);
    }

    100% {
        transform:
            translateY(-15vh)
            translateX(-30px)
            rotate(360deg);

        opacity: 0;
    }
}

@keyframes confettiFall {

    0% {
        transform:
            translateY(-20px)
            rotate(0deg);

        opacity: 0;
    }

    10% {
        opacity: 1;
    }

    100% {
        transform:
            translateY(110vh)
            rotate(720deg);

        opacity: 0;
    }
}

@keyframes sparkleAnim {

    0%,100% {
        opacity: 0;
        transform: scale(.5);
    }

    50% {
        opacity: 1;
        transform: scale(1.4);
    }
}


/* =========================
   MOBILE
========================= */

@media(max-width:600px) {

    .normal-title {
        font-size: 30px;
    }

    .normal-text {
        font-size: 16px;
    }

    .countdown {
        font-size: 80px;
    }

    .message {
        font-size: 15px;
        padding: 18px;
    }
}

</style>
</head>


<body>


<!-- =========================
     MÀN 1
========================= -->

<section id="screen1" class="screen active">

    <div class="normal-title">
        Thảo ơi
    </div>

    <div class="normal-text">
        Có cái này muốn cho Thảo xem.
    </div>

    <button class="button" onclick="startSurprise()">
        Mở ra
    </button>

</section>


<!-- =========================
     MÀN 2
========================= -->

<section id="screen2" class="screen">

    <div class="loading-title">
        Đang chuẩn bị...
    </div>

    <div class="loading-box">
        <div class="loading-bar" id="loadingBar"></div>
    </div>

    <div class="loading-percent" id="percent">
        0%
    </div>

    <button
        class="button"
        id="continueBtn"
        onclick="goCountdown()"
    >
        Tiếp tục
    </button>

</section>


<!-- =========================
     MÀN 3
========================= -->

<section id="screen3" class="screen">

    <div class="countdown" id="countdown">
        3
    </div>

</section>


<!-- =========================
     MÀN CUỐI
========================= -->

<section id="screen4" class="screen">

    <div class="final-small">
        ✨ HÔM NAY CÓ MỘT NGƯỜI ĐẶC BIỆT ✨
    </div>

    <div class="final-title">
        Happy Birthday
    </div>

    <div class="final-name">
        Thảo 💗
    </div>

    <div class="cake">
        🎂
    </div>

    <div class="message">

        Chúc Thảo sinh nhật vui vẻ, tuổi mới luôn vui vẻ
        và hạnh phúc nhé 💕
        <br>

        Mong những điều tốt đẹp nhất sẽ luôn đến với Thảo.
        <br>

        <strong>
            Happy Birthday cô gái đáng yêu 🎀💗
        </strong>

    </div>

</section>


<script>

/* =========================
   CHUYỂN MÀN HÌNH
========================= */

function showScreen(id) {

    document.querySelectorAll(".screen")
        .forEach(screen => {
            screen.classList.remove("active");
        });

    document.getElementById(id)
        .classList.add("active");
}


/* =========================
   BẮT ĐẦU
========================= */

function startSurprise() {

    showScreen("screen2");

    const bar =
        document.getElementById("loadingBar");

    const percent =
        document.getElementById("percent");

    const button =
        document.getElementById("continueBtn");

    let value = 0;

    setTimeout(() => {

        bar.style.width = "100%";

    }, 100);

    const timer =
        setInterval(() => {

            value++;

            percent.innerText =
                value + "%";

            if(value >= 100) {

                clearInterval(timer);

                setTimeout(() => {

                    button.style.display =
                        "block";

                }, 500);

            }

        }, 25);
}


/* =========================
   COUNTDOWN
========================= */

function goCountdown() {

    showScreen("screen3");

    let number = 3;

    const counter =
        document.getElementById("countdown");

    counter.innerText = number;

    const timer =
        setInterval(() => {

            number--;

            if(number > 0) {

                counter.innerText =
                    number;

                counter.style.animation =
                    "none";

                void counter.offsetWidth;

                counter.style.animation =
                    "countdownPop 1s ease";

            } else {

                clearInterval(timer);

                counter.innerText =
                    "🎁";

                setTimeout(() => {

                    showScreen("screen4");

                    startCelebration();

                }, 900);

            }

        }, 1000);
}


/* =========================
   HIỆU ỨNG CUỐI
========================= */

const emojis = [
    "💗",
    "💕",
    "💖",
    "💝",
    "❤️",
    "💘",
    "🌸",
    "🌷",
    "🌹",
    "🎀",
    "✨",
    "⭐",
    "🎈"
];


function createFloating() {

    const item =
        document.createElement("div");

    item.className =
        "floating";

    item.innerText =
        emojis[
            Math.floor(
                Math.random() * emojis.length
            )
        ];

    item.style.left =
        Math.random() * 100 + "vw";

    item.style.fontSize =
        (18 + Math.random() * 35) + "px";

    item.style.animationDuration =
        (5 + Math.random() * 7) + "s";

    document.body.appendChild(item);

    setTimeout(() => {

        item.remove();

    }, 13000);
}


function createConfetti() {

    const item =
        document.createElement("div");

    item.className =
        "confetti";

    item.style.left =
        Math.random() * 100 + "vw";

    item.style.background =
        `hsl(${Math.random() * 360}, 80%, 70%)`;

    item.style.animationDuration =
        (3 + Math.random() * 4) + "s";

    document.body.appendChild(item);

    setTimeout(() => {

        item.remove();

    }, 8000);
}


function createSparkle() {

    const item =
        document.createElement("div");

    item.className =
        "sparkle";

    item.innerText = "✦";

    item.style.left =
        Math.random() * 100 + "vw";

    item.style.top =
        Math.random() * 100 + "vh";

    item.style.animationDelay =
        Math.random() * 2 + "s";

    document.body.appendChild(item);

    setTimeout(() => {

        item.remove();

    }, 3000);
}


/* =========================
   BÙNG NỔ
========================= */

function startCelebration() {

    /* Đợt đầu thật nhiều */

    for(let i = 0; i < 80; i++) {

        setTimeout(() => {
            createFloating();
        }, i * 30);

    }

    for(let i = 0; i < 100; i++) {

        setTimeout(() => {
            createConfetti();
        }, i * 20);

    }

    for(let i = 0; i < 40; i++) {

        setTimeout(() => {
            createSparkle();
        }, i * 80);

    }


    /* Sau đó tiếp tục bay */

    setInterval(
        createFloating,
        350
    );

    setInterval(
        createConfetti,
        180
    );

    setInterval(
        createSparkle,
        500
    );
}

</script>

</body>
</html>
:::
